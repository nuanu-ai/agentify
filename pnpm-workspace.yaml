# One product, one reproducible install: every package of it is installed from
# this workspace and its lockfile, code and documentation alike, and one linter
# reads all of it.
packages:
  - packages/*
  - apps/*

allowBuilds:
  core-js: false
  esbuild: true
  sharp: true
