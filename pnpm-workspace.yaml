# One product, one reproducible install: every package of it is installed from
# this workspace and its lockfile, code and documentation alike; one linter
# reads all of it and every package compiles from one base.
packages:
  - packages/*
  - apps/*

allowBuilds:
  core-js: false
  esbuild: true
  sharp: true
