/**
 * The names of the two controls a message about the payout wallet tells a
 * merchant to press, as the screens draw them.
 *
 * One place for both readers: the screen that draws the button and the
 * message that names it. A button renamed on a screen is renamed in the
 * message with it, rather than leaving a message that sends somebody looking
 * for a control by a name nothing shows.
 */

/** The merchant-wide pause on the Cards screen. */
export const STOP_ALL_SELLING = "Stop all selling";

/** The account's other sessions ended, on the Settings screen. */
export const SIGN_OUT_EVERY_OTHER_DEVICE = "Sign out every other device";
