# The operator's own inventory: the TEST fixture host the Woo acceptance route
# is installed on, its login name and the addresses of the shared ingress.
# Deployment facts, not source. The tracked neighbours are inventory.yml (structure only) and
# inventory.local.yml.example (the shape to fill in).
inventory.local.yml
