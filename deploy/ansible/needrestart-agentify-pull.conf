# The Agentify deployment pull agent is a oneshot service that runs a release.
# Restarting it mid-run kills a deployment after its recovery boundary, so
# needrestart never restarts it; the timer starts a fresh process every minute
# anyway, and that process loads the upgraded libraries.
$nrconf{override_rc}{qr(^agentify-pull@)} = 0;
