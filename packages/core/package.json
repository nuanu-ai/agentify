{
  "name": "@agentify/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agentify domain core — orders, receipts, the state machine; pure logic with no IO",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@nuanu-ai/agentify-contracts": "workspace:*",
    "vitest": "^4.1.11"
  }
}
